generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// WorkflowStep.actorUser uses the named WorkflowStepActor relation.
// The corresponding User.workflowActorSteps relation is declared on User.
