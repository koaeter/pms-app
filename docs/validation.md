# Repository validation

This file documents the validation baseline for the PMS foundation branch.

- Prisma schema validation runs in GitHub Actions.
- Prisma formatting is checked in CI.
- Application implementation should proceed only after the schema baseline passes validation.
